import { Meta, ArgTypes, Title } from '@storybook/blocks';
import * as AccordionContent from './AccordionContent.stories.js';

<Meta of={AccordionContent} />

<Title>AccordionContent</Title>

<span className="badge sb-unstyled">Core</span>

## Overview
The `AccordionContent` is a core Finblox-UI component used as a sub-component to construct a classic accordion style ui.
It's role is to function as collapsible element within an accordion item to display the accordion item content.

See [&lt;AccordionRoot /&gt;](?path=/docs/library-core-accordion--docs) for an example.

<ArgTypes />
