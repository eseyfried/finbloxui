import { Meta, ArgTypes, Title } from '@storybook/blocks';
import * as AccordionHeader from './AccordionHeader.stories.js';

<Meta of={AccordionHeader} />

<Title>AccordionHeader</Title>

<span className="badge sb-unstyled">Core</span>

## Overview
The `AccordionTrigger` is a core Finblox-UI component used as a sub-component to construct a classic accordion style ui.
It's role is to function as a trigger button to expand/collapse an accordtion item's content section.

See [&lt;AccordionRoot /&gt;](?path=/docs/library-core-accordion--docs) for an example.

<ArgTypes />
