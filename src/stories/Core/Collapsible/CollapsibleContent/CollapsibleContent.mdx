import { Meta, ArgTypes, Title } from '@storybook/blocks';
import * as CollapsibleContent from './CollapsibleContent.stories.js';

<Meta of={CollapsibleContent} />

<Title>CollapsibleContent</Title>

<span className="badge sb-unstyled">Core</span>

## Overview
The `CollapsibleContent` is a core Finblox-UI component used as a sub-component to construct a classic collapsible style ui.

See [&lt;CollapsibleRoot /&gt;](?path=/docs/library-core-collapsible--docs) for an example.

<ArgTypes />
