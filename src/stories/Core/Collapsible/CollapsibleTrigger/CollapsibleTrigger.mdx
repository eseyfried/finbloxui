import { Meta, ArgTypes, Title } from '@storybook/blocks';
import * as CollapsibleTrigger from './CollapsibleTrigger.stories.js';

<Meta of={CollapsibleTrigger} />

<Title>CollapsibleTrigger</Title>

<span className="badge sb-unstyled">Core</span>

## Overview
The `CollapsibleTrigger` is a core Finblox-UI component used as a sub-component to construct a classic collapsible style ui.

See [&lt;CollapsibleRoot /&gt;](?path=/docs/library-core-collapsible--docs) for an example.

<ArgTypes />
